using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.Serialization;
using UnityEngine.XR.Interaction.Toolkit;
using XRController = UnityEngine.InputSystem.XR.XRController;

public class RayController : MonoBehaviour
{
        /*public XRController rightRaycast;
        public XRController leftRaycast;
        public InputHelpers.Button raycastActivatorButton;
        public float activationThreshold = 0.1f;

        public bool CheckIfActive(XRController controller)
        {
            InputHelpers.IsPressed(controller.inputDevice, raycastActivatorButton, out bool isActivated, 
                activationThreshold);

              return isActivated;
        }
        void Update()
        {
            if (leftRaycast || rightRaycast)
            {
                leftRaycast.gameObject.SetActive(CheckIfActive(rightRaycast));
                rightRaycast.gameObject.SetActive(CheckIfActive(rightRaycast));
            }
        
        }*/
        
}

