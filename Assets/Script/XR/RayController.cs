using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.Serialization;
using UnityEngine.XR.Interaction.Toolkit;


public class RayController : MonoBehaviour
{
       /* public XRRayInteractor _rayShoot;
        //public XRController leftRaycast;
        //public InputHelpers.Button raycastActivatorButton;
        public float activationThreshold = 0.1f;

        public bool CheckIfActive(XRRayInteractor interactor)
        {
            InputHelpers.IsPressed(interactor, raycastActivatorButton, out bool isActivated, 
                activationThreshold);

              return isActivated;
        }
        void Update()
        {
            if (_rayShoot)
            {
                _rayShoot.gameObject.SetActive(CheckIfActive(_rayShoot));
                
            }
        
        }*/
        
}

